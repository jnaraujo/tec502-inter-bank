services:
  client:
    build: 
      context: ./client
      args:
        - VITE_BANK_URL=http://localhost:3001
    networks:
      - iot-network
    ports:
      - "3000:3000"

networks:
  iot-network:
    driver: bridge